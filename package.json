cat > package.json << 'EOF'
{
  "name": "photos-api-local",
  "version": "1.0.0",
  "description": "Photos API with local JSON file storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "minimal": "node server-minimal.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "dotenv": "^16.0.3"
  }
}
EOF
